{{ form_start(form) }}


{{ form_row(form.firstname) }}

{{ form_row(form.lastname) }}

{{ form_row(form.phone) }}

{{ form_row(form.email) }}

{{ form_row(form.message) }}


      {{ form_rest(form) }}
      <br>
      <button type="submit" class="btn btn-primary">Valider votre message</button>

{{ form_end(form) }}

