{% extends 'base.html.twig' %}

{% block title %}Contact2{% endblock %}

{% block body %}
    <h1>Contact2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contact2.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ contact2.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ contact2.lastname }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ contact2.phone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ contact2.email }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ contact2.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contact2_index') }}">back to list</a>

    <a href="{{ path('contact2_edit', {'id': contact2.id}) }}">edit</a>

    {{ include('contact2/_delete_form.html.twig') }}
{% endblock %}
